Renders real marks from assets/brand/. Set `base` to reach project root; `invert` for dark surfaces.

```jsx
<BrandMark mark="logo-eye" height={40} base="../../" />
<BrandMark mark="eye" height={64} base="../../" invert />
```
